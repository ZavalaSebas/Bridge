<UserControl x:Class="Bridge.Views.StatisticsOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:Bridge.Converters"
             xmlns:art="clr-namespace:Bridge.Assets"
             xmlns:res="clr-namespace:Bridge.Resources">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Bridge;component/Styles/MainWindowResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,28">
            <Border CornerRadius="10"
                    Background="{DynamicResource Bridge.Card.Background}"
                    BorderBrush="{DynamicResource Bridge.SeparatorBrush}"
                    BorderThickness="1"
                    Padding="16"
                    Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <Border Width="56" Height="56" CornerRadius="28" ClipToBounds="True"
                            BorderBrush="{DynamicResource Bridge.SeparatorBrush}" BorderThickness="1">
                        <Image Stretch="UniformToFill"
                               Source="{Binding UserProfile, Converter={StaticResource UserProfileAvatarConverter}, ConverterParameter=112}"/>
                    </Border>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding UserProfile.DisplayName}"
                                   FontSize="18"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="{x:Static res:Strings.Statistics}"
                                   Foreground="{StaticResource TextFillColorSecondaryBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel DataContext="{Binding Statistics}">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <ui:SymbolIcon Symbol="ChartMultiple24" FontSize="22" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                <TextBlock Text="{x:Static res:Strings.Statistics}" FontSize="{StaticResource Bridge.FontSize.Title}" FontWeight="SemiBold" Margin="10,0,0,0"/>
            </StackPanel>
            <WrapPanel Margin="0,0,0,16">
                <Border Width="210" Padding="14" Margin="0,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.All}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding TotalCount, Converter={StaticResource GamesCount}}" FontWeight="SemiBold" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Width="210" Padding="14" Margin="4,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.Installed}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <Grid Margin="0,2,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding InstalledCount}" FontWeight="SemiBold" FontSize="15"/>
                            <TextBlock Grid.Column="1" Text="{Binding InstalledPercent, StringFormat='{}{0:0}%'}" FontSize="13" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Bottom" Margin="8,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Width="210" Padding="14" Margin="0,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.NotInstalled}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding NotInstalledCount}" FontWeight="SemiBold" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Width="210" Padding="14" Margin="4,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.Hidden}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding HiddenCount}" FontWeight="SemiBold" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Width="210" Padding="14" Margin="0,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.Favorites}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding FavoriteCount}" FontWeight="SemiBold" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
            <TextBlock Text="{x:Static res:Strings.Playtime}" FontWeight="SemiBold" FontSize="{StaticResource Bridge.FontSize.Heading}" Foreground="{StaticResource TextFillColorSecondaryBrush}" Margin="0,0,0,6"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Border Padding="14" Margin="0,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.Total}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding TotalPlaytimeDisplay}" FontWeight="SemiBold" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Padding="14" Margin="4,4,0,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.Average}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding AveragePlaytimeDisplay}" FontWeight="SemiBold" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
            <TextBlock Text="{x:Static res:Strings.Completion}" FontWeight="SemiBold" FontSize="{StaticResource Bridge.FontSize.Heading}" Foreground="{StaticResource TextFillColorSecondaryBrush}" Margin="0,0,0,6"/>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <Border Padding="14" Margin="0,4,4,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.NotPlayedFilter}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <Grid Margin="0,2,0,0">
                            <TextBlock FontWeight="SemiBold" FontSize="15">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1:0}%)">
                                        <Binding Path="NotPlayedCount"/>
                                        <Binding Path="NotPlayedPercent"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Padding="14" Margin="4,4,0,0" CornerRadius="8" Background="{DynamicResource Bridge.Card.Background}">
                    <StackPanel>
                        <TextBlock Text="{x:Static res:Strings.Played}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        <Grid Margin="0,2,0,0">
                            <TextBlock FontWeight="SemiBold" FontSize="15">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1:0}%)">
                                        <Binding Path="PlayedCount"/>
                                        <Binding Path="PlayedPercent"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>
            </UniformGrid>
            <TextBlock Text="{x:Static res:Strings.TopPlayTime}" FontWeight="SemiBold" FontSize="{StaticResource Bridge.FontSize.Heading}" Foreground="{StaticResource TextFillColorSecondaryBrush}" Margin="0,0,0,6"/>
            <ListBox ItemsSource="{Binding TopPlayed}" Background="Transparent" BorderThickness="0" Padding="0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource Bridge.Accent.Hover}"/></Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource TextFillColorPrimaryBrush}"/>
                                <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Bridge.SystemAccentBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Height="24" Width="24" Stretch="Uniform" converters:CachedImage.SourceUrl="{Binding Icon}" converters:CachedImage.FallbackArtwork="{x:Static art:GameArtworkFallback.Icon}" RenderOptions.BitmapScalingMode="Fant"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="{StaticResource Bridge.FontSize.BodySmall}" Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="{Binding PlaytimeSeconds, Converter={StaticResource PlaytimeConverter}}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
