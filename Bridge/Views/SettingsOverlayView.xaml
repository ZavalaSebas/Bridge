<UserControl x:Class="Bridge.Views.SettingsOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:res="clr-namespace:Bridge.Resources">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,28">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <ui:SymbolIcon Symbol="Settings24" FontSize="22" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                <TextBlock Text="{x:Static res:Strings.Settings}" FontSize="{StaticResource Bridge.FontSize.Title}" FontWeight="SemiBold" Margin="10,0,0,0"/>
            </StackPanel>
            <TextBlock Text="{x:Static res:Strings.SettingsDescription}" Foreground="{StaticResource TextFillColorSecondaryBrush}" Margin="0,0,0,20"/>
            <WrapPanel>
                <Button MinWidth="260" Margin="0,0,8,8" Padding="14,12" Click="ConfigureEmulator_Click" HorizontalContentAlignment="Left" Background="{DynamicResource Bridge.Card.Background}" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="{x:Static res:Strings.ConfigureEmulatorTitle}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Box24" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Static res:Strings.ConfigureEmulatorTitle}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button MinWidth="260" Margin="0,0,8,8" Padding="14,12" Click="IgdbSettings_Click" HorizontalContentAlignment="Left" Background="{DynamicResource Bridge.Card.Background}" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="{x:Static res:Strings.IgdbSettingsTitle}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Database24" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Static res:Strings.IgdbSettingsTitle}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button MinWidth="260" Margin="0,0,8,8" Padding="14,12" Click="CustomThemeColor_Click" HorizontalContentAlignment="Left" Background="{DynamicResource Bridge.Card.Background}" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="{x:Static res:Strings.CustomThemeColorTitle}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Color24" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Static res:Strings.CustomThemeColorTitle}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button MinWidth="260" Margin="0,0,8,8" Padding="14,12" Command="{Binding CheckForUpdatesCommand}" HorizontalContentAlignment="Left" Background="{DynamicResource Bridge.Card.Background}" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="{x:Static res:Strings.CheckForUpdates}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ArrowSync24" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Static res:Strings.CheckForUpdates}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button MinWidth="260" Margin="0,0,8,8" Padding="14,12" Click="About_Click" HorizontalContentAlignment="Left" Background="{DynamicResource Bridge.Card.Background}" BorderThickness="0" Cursor="Hand" AutomationProperties.Name="{x:Static res:Strings.AboutBridge}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Info24" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="{x:Static res:Strings.AboutBridge}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </WrapPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
