<ui:FluentWindow x:Class="Bridge.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Bridge.Controls"
        xmlns:res="clr-namespace:Bridge.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        x:Name="Root"
        Title="{x:Static res:Strings.AboutBridgeTitle}"
        Width="540" Height="620"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{StaticResource TextFillColorPrimaryBrush}"
        FontFamily="{DynamicResource Bridge.FontFamily}"
        FontSize="{StaticResource Bridge.FontSize.Body}">
    <ui:FluentWindow.Resources>
        <DataTemplate x:Key="AboutLinkCardTemplate">
            <Button Click="LinkRow_Click" Tag="{Binding Url}" Padding="0" Margin="2,0,2,6" HorizontalContentAlignment="Stretch" Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="8" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" Padding="10,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:SymbolIcon Grid.Column="0" Symbol="{Binding Icon}" FontSize="18" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" VerticalAlignment="Top" Margin="0,1,10,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Label}" FontWeight="Medium" FontSize="13" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Detail}" FontSize="11" Foreground="{StaticResource TextFillColorSecondaryBrush}" Margin="0,2,0,0" TextWrapping="Wrap" MaxHeight="34" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <ui:SymbolIcon Grid.Column="2" Symbol="ArrowUpRight24" FontSize="14" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Top" Margin="4,1,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22007ACC"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </DataTemplate>
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <Grid Grid.RowSpan="2" IsHitTestVisible="False">
            <Grid.CacheMode><BitmapCache/></Grid.CacheMode>
            <controls:FadeImage x:Name="BackgroundArt" Stretch="UniformToFill" Width="{Binding ActualWidth, ElementName=Root}" Height="{Binding ActualHeight, ElementName=Root}"><controls:FadeImage.Effect><BlurEffect Radius="{StaticResource Bridge.Background.BlurRadius}" KernelType="Gaussian" RenderingBias="Performance"/></controls:FadeImage.Effect></controls:FadeImage>
            <Border Background="{StaticResource Bridge.Background.DarkOverlay}"/>
        </Grid>

        <ui:TitleBar Grid.Row="0" Background="{DynamicResource Bridge.Sidebar.Background}" BorderBrush="{DynamicResource Bridge.SeparatorBrush}" BorderThickness="0,0,0,1" Height="56" ShowMinimize="False" ShowMaximize="False" ShowClose="True"><ui:TitleBar.Header>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                <ui:SymbolIcon Symbol="Info24" FontSize="18" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{x:Static res:Strings.AboutBridgeTitle}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </ui:TitleBar.Header></ui:TitleBar>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="20,12,20,20">
            <StackPanel MaxWidth="452" HorizontalAlignment="Center">

                <!-- Hero: logo + nombre + versión -->
                <StackPanel Orientation="Horizontal" Margin="0,4,0,18">
                    <Image Source="{StaticResource Bridge.LogoImage}"
                           Width="64" Height="64"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{x:Static res:Strings.BridgeAppName}" FontSize="{StaticResource Bridge.FontSize.TitleLarge}" FontWeight="SemiBold"/>
                        <Border Margin="0,3,0,0" Padding="8,2" CornerRadius="10" HorizontalAlignment="Left" Background="{DynamicResource ControlFillColorSecondaryBrush}">
                            <TextBlock x:Name="VersionText" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <!-- Descripción -->
                <TextBlock Text="{x:Static res:Strings.AboutDescription}" TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}" LineHeight="17" Margin="0,0,0,16"/>

                <!-- Créditos -->
                <TextBlock Text="{x:Static res:Strings.Credits}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" Margin="2,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="2,0,0,16">
                    <ui:SymbolIcon Symbol="PaintBrush24" FontSize="15" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="{x:Static res:Strings.AboutArtCredit}" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Proyecto -->
                <TextBlock Text="{x:Static res:Strings.Project}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" Margin="2,0,0,8"/>
                <ItemsControl x:Name="ProjectLinks"
                              ItemTemplate="{StaticResource AboutLinkCardTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <!-- Soporte -->
                <TextBlock Text="{x:Static res:Strings.Support}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" Margin="2,10,0,8"/>
                <ItemsControl x:Name="SupportLinks"
                              ItemTemplate="{StaticResource AboutLinkCardTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <!-- Entorno -->
                <TextBlock Text="{x:Static res:Strings.Environment}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" Margin="2,10,0,8"/>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="2,0,0,6">
                        <ui:SymbolIcon Symbol="Apps24" FontSize="15" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock x:Name="RuntimeText" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                        <ui:SymbolIcon Symbol="Globe24" FontSize="15" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock x:Name="OsText" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>

                <!-- Disclaimer legal -->
                <TextBlock Text="{x:Static res:Strings.AboutDisclaimer}" TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" Margin="2,16,0,0" LineHeight="15"/>

                <Button Content="{x:Static res:Strings.Close}" IsCancel="True" IsDefault="True" HorizontalAlignment="Center" Width="100" Margin="0,20,0,0" Padding="0,7"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>