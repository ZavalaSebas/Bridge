<Window x:Class="Bridge.ScanRomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Scan ROMs" Height="180" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="Folder to scan for ROMs" />
        <TextBox x:Name="FolderBox" Margin="0,0,0,12" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" IsCancel="True" />
            <Button Content="Scan" Width="80" Margin="8,0,0,0" IsDefault="True" Click="Scan_Click" />
        </StackPanel>
    </StackPanel>
</Window>
